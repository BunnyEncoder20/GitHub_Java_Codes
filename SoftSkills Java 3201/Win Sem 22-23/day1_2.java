import java.util.*;

// Java program to check if the inputed numbers are a palindrome or not 
public class day1_2 {
    public static void main(String[] args) {
        LinkedList<Integer> list = new LinkedList<>();

        Scanner op = new Scanner(System.in);
        int num = op.nextInt();
        while (num--!=0) {
            list.add(s.nextInt());
        }

        LinkedList<Integer> revlist = new LinkedList<>();
        revlist = Collections.reverse(list);

        .
        .
        .

        op.close();
    }
}
